#ifndef CALC_H
#define CALC_h

int add(int x, int y);
int sub(int x, int y);
int mul(int x, int y);
int div(int x, int y);
int mod(int x, int y);
int power_two(int x);
int power(int x, int n);

#endif
